---
description: "Git operations and commands for version control of the ArchiveRetention project"
globs: 
alwaysApply: false
---
# Git Operations

## Basic Commands
```bash
git add .
git commit -m "Update script with improved error handling"
git status
git push origin main
```

## Configuration
```bash
# Current configuration
git config user.name "paraserv"
git config user.email "6347244+paraserv@users.noreply.github.com"
git config user.signingkey BA7AFC3EFB31E553
git config commit.gpgsign true
```

## Remote Management
```bash
# Current remote
git remote -v
# origin  git@github.com:paraserv/LRArchiveRetention.git (fetch)
# origin  git@github.com:paraserv/LRArchiveRetention.git (push)

# Update remote if needed
git remote set-url origin git@github.com:paraserv/LRArchiveRetention.git
```

## Branching
```bash
# Current branches: main, fix-commits
git checkout main
git pull origin main

# Create feature branch
git checkout -b feature/new-feature
git push -u origin feature/new-feature
```

## Pull Settings
```bash
# Current setting
git config pull.rebase false  # Use merge when pulling
```
